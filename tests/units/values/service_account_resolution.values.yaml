nameOverride: platform

deployments:
  api:
    serviceAccountName: deployer
    containers:
      - name: api
        image: nginx
        imageTag: stable

serviceAccount:
  deployer:
    role:
      name: deployer-role
      rules:
        - apiGroups:
            - ""
          resources:
            - pods
          verbs:
            - get
